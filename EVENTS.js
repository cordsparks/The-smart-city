document.addEventListener("DOMContentLoaded", function() {
    console.log("Events page loaded successfully.");

    // Notify users of upcoming events
    setTimeout(() => {
        alert("Stay tuned for more exciting events coming soon! 🚀");
    }, 2000);
});
